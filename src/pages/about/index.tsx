import React from 'react';
import Style from './index.module.css';

export default () => {
  <div className={Style.pageAbout}>
    {/* <p>{currentLang?.about.name}</p>
    <p>{currentLang?.about.name}</p>
    <p>{currentLang?.about.name}</p> */}
  </div>;
};
